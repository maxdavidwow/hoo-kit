1. first argument is the path to the hookit.json, if omitted the working directory will be used

options:
	// flag which indicates if the ui server will be run
	// default: true
	--runUiServer: true | false

	// if passed the server will use the html file at the given path
	--customUiPath: string

	// if passed the ui-server will use the given address
	// default: 'localhost:8080'
	--uiServerAddress: string

	// if passed the ui-server will use the given address for the websocket
	// default: 'localhost:8081'
	--uiServerWebsocketAddress: string